services:
  - type: web
    name: carbonmetric
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: MAIL_USERNAME
        value: ""
      - key: MAIL_PASSWORD
        value: ""
